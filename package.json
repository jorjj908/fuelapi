{
  "name": "hull-fuel-daily",
  "version": "1.0.0",
  "description": "Daily job that emails the cheapest petrol within 15 miles of Hull",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "start": "node dist/src/run.js",
    "run-once": "npm run clean && npm run build && npm start"
  },
  "dependencies": {
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0"
  }
}
